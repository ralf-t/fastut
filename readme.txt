https://www.fastapitutorial.com/blog/fastapi-route/
Follows a project design pattern called repository pattern

To avoid pre-commit hooks use:: git commit -m "my message" --no-verify

To test codebase syntax:
- 'pre-commit run --all-files'

To test:
- 'pytest'
